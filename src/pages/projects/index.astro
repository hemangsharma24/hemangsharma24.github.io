---
import Layout from "@/layouts/Layout.astro";
import FeaturedCard from "@/components/FeaturedCard.astro";
import { getCollection } from "astro:content";

const projects = (await getCollection("projects")).sort((a, b) => a.data.order - b.data.order);
---

<Layout title="Projects" description="Case studies by Hemang Sharma — UI/UX Product Designer.">

  <!-- Header -->
  <section style={`max-width: var(--max-w-page); margin: 0 auto; padding: var(--section-gap) var(--px-page) 4rem;`}>
    <p style={`font-family: var(--font-mono); font-size: var(--text-label); color: var(--color-muted); letter-spacing: var(--tracking-wide); text-transform: uppercase; margin: 0 0 2rem;`}>Selected Work</p>
    <h1 style={`font-family: var(--font-display); font-size: clamp(2.25rem, 8vw, var(--text-h1)); letter-spacing: var(--tracking-tight); line-height: var(--leading-tight); margin: 0; color: var(--color-primary);`}>
      Featured Projects
    </h1>
  </section>

  <!-- Project cards -->
  <section style={`max-width: var(--max-w-page); margin: 0 auto; padding: 0 var(--px-page) 4rem;`}>
    <div style="display: flex; flex-direction: column; gap: 1.25rem;">
      {projects.map((project, i) => (
        <FeaturedCard
          number={project.data.number}
          title={project.data.title}
          tags={project.data.type}
          blurb={project.data.blurb}
          href={`/projects/${project.data.slug}`}
          theme={project.data.theme}
          category={project.data.category}
          imageRight={i % 2 === 0}
        />
      ))}
    </div>
  </section>

  <!-- Confidential note -->
  <div style="margin: 0 0 4rem; border-top: 1px solid #B78191; border-bottom: 1px solid #B78191; background: #F7F1E9;">
    <div style={`max-width: var(--max-w-page); margin: 0 auto; padding: 2rem var(--px-page); display: flex; gap: 1rem; align-items: flex-start;`}>
      <span style="font-size: 1rem; flex-shrink: 0; background: #E8A838; border-radius: 0.35rem; width: 2rem; height: 2rem; display: flex; align-items: center; justify-content: center; margin-top: 0.1rem;">🔒</span>
      <div>
        <p style={`font-family: var(--font-body); font-size: var(--text-body); font-weight: 600; margin: 0 0 0.35rem; color: #321D00;`}>
          Not everything I've built can be shown publicly
        </p>
        <p style={`font-family: var(--font-body); font-size: var(--text-body); line-height: var(--leading-body); color: #321D00; margin: 0; opacity: 0.65;`}>
          A number of projects across Enterprise, AI and Internal platforms are intentionally not displayed publicly due to project confidentiality and product restrictions
        </p>
      </div>
    </div>
  </div>

  <!-- Off the grid -->
  <div style="background: #FFEFF4; border-top: 1px solid var(--color-border);">
    <div class="bio-inner" style={`max-width: 70rem; margin: 0 auto; padding: 3rem var(--px-page); text-align: center;`}>
      <div style="margin: 0 auto 2rem;">
        <img src="/home_page_assets/Hero Section/Music-Headphones-Human--Streamline-Pixel.svg" width="48" height="48" alt="" style="display: block; margin: 0 auto;" />
      </div>
      <p style={`font-family: var(--font-body); font-size: var(--text-body-lg); line-height: var(--leading-body); color: var(--color-ink); font-style: italic; margin: 0 0 1.5rem; opacity: 0.8;`}>
        Off the grid, I'm a music explorer who enjoys Linkin Park, Radiohead and Jagjit Singh, a Football fanatic, and a Movie buff with a very long watchlist. I also enjoy writing philosophical thoughts, short poems, and reflections inspired by everyday life.
      </p>
      <p style={`font-family: var(--font-body); font-size: var(--text-body); line-height: var(--leading-body); color: var(--color-muted); margin: 0 0 2.5rem;`}>
        I'm open to freelance opportunities, collaborations, and meaningful conversations around design, AI products, cinema or ideas worth building. Click the email below to copy it, and feel free to reach out anytime.
      </p>
      <button
        onclick="navigator.clipboard.writeText('kshamahemang@gmail.com')"
        class="bio-email-btn"
        style={`
          font-family: var(--font-body); font-size: var(--text-body);
          color: var(--color-cream); background: var(--color-ink);
          border: none; cursor: pointer;
          padding: 0.875rem 2rem;
          border-radius: 2rem;
          display: inline-flex; align-items: center; gap: 0.75rem;
        `}
      >
        kshamahemang@gmail.com
        <span style="font-size: 0.9rem; opacity: 0.7;">|</span>
        <img src="/home_page_assets/Footer/Copy.svg" width="16" height="16" alt="" style="display: block; opacity: 0.7;" />
      </button>
    </div>
  </div>

</Layout>

<style>
  @media (max-width: 639px) {
    .bio-inner {
      padding-left: 1.25rem !important;
      padding-right: 1.25rem !important;
    }

    .bio-email-btn {
      font-size: 0.875rem !important;
      padding: 0.75rem 1.25rem !important;
      word-break: break-all;
    }
  }
</style>
